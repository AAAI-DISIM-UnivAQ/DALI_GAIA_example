start_evacuationE(X) :> guide_people_to_exitA(X), reportA(evacuation_done).

guide_people_to_exitA(X) :> write('Guiding people to '), write(X), nl.

reportA(evacuation_done) :> messageA(coordinator, send_messageA(evacuation_done, Me)).